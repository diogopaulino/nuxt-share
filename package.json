{
  "name": "root",
  "private": true,
  "devDependencies": {
    "eslint": "^7.25.0",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "lerna bootstrap",
    "run-all": "lerna run --parallel dev"
  }
}
